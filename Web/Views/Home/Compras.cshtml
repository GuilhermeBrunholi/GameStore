<div class="row">
    <div class="col-md-12">
        <table class="table table-hover text-white">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Preço</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in @ViewBag.produtos)
                {
                    <tr>
                        <td>@item.nome </td>
                        <td>R$: @item.preco </td>
                        <td>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>